# 安全与隐私

## 不要公开提交

不要在 Issue、日志、截图、Pull Request 或提交记录中提供以下内容：

- 微信数据库密钥、图片 XOR/AES 密钥或 `secrets.dat`
- 真实 wxid、微信数据库、CipherTalk 配置数据库
- 聊天内容、联系人、消息 Excel、图片及媒体缓存
- 包含用户名的本机绝对路径

如果这些内容曾经进入 Git 历史，仅删除当前文件并不够。应立即视为已经泄露，轮换可轮换的凭据，并清理完整历史和所有 Release 资产。

## 负责任使用

本项目仅用于本人账号以及操作者明确获授权处理的数据。不得用于未授权访问、监控他人账号、规避访问控制或秘密收集第三方消息。公开代码不代表获得微信、CipherTalk、影刀或其他软件的额外授权。

## 报告安全问题

请优先使用 GitHub 的 Private vulnerability reporting，不要在公开 Issue 中粘贴敏感数据。报告只需提供最小复现步骤、版本和脱敏后的错误码；不要发送密钥或真实消息样本。
