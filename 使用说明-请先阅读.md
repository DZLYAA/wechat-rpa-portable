# 微信消息采集便携版使用说明

> 适用对象：收到 `wechat-rpa-portable` 文件夹和影刀流程文件的新用户。  
> 建议按本文顺序操作，不要跳过环境检测和首次基线测试。

## 一、使用前须知

本工具用于采集**当前电脑上、当前 Windows 用户登录的本人微信账号**的新消息，并增量保存到 Excel。

请仅处理你本人账号以及你有权处理的数据。消息可能包含联系人姓名、聊天内容、图片和文件路径，不要把生成的 Excel、微信数据库、密钥或配置文件转发给无关人员。

本工具包不包含微信、CipherTalk、影刀或 Microsoft Excel 软件本体。微信或 CipherTalk 升级后，接口和数据库格式可能发生变化；升级前建议先备份并小范围测试。

## 二、你会收到什么

通常包含两部分：

1. `wechat-rpa-portable` 文件夹或同名 ZIP：采集程序、配置工具、影刀模块、Excel 模板。
2. 影刀流程文件：负责循环执行采集、写入 Excel 和确认消息。

便携文件夹中的关键文件：

| 文件 | 用途 |
|---|---|
| `setup.exe` | 首次配置本机路径、wxid 和密钥 |
| `doctor.exe` | 检测配置、文件和 CipherTalk 本地接口 |
| `collect_silent.exe` | 静默采集本轮新消息 |
| `ack_silent.exe` | Excel 保存成功后确认本轮消息 |
| `shadowbot\module1.py` | 影刀自定义模块代码 |
| `data\wechat_messages_template.xlsx` | 空白 Excel 模板 |

## 三、准备软件

在同一个 Windows 用户下准备并运行：

1. Windows 10 或 Windows 11。
2. 微信桌面版：登录需要采集的本人微信账号。
3. CipherTalk 桌面版：本包不附带安装程序；运行根目录的 `1-安装密语.exe` 后会从官方 GitHub Release 下载并核验 SHA-256。
4. 影刀客户端：建议使用与流程制作版本相同或更高的版本。
5. Microsoft Excel 桌面版（推荐但非强制）：没有 Excel COM 时，模块会尝试使用影刀 Python 自带的 `openpyxl` 写入。

本包默认使用 CipherTalk 桌面版，不需要接收者另外安装 Node.js，也不使用曾出现 `wcdb_init -1006` 的旧版全局 `miyu` CLI。

## 四、安装密语 CipherTalk

如果当前电脑尚未安装 CipherTalk：

1. 双击便携文件夹根目录的 `1-安装密语.exe`。
2. 阅读非商业许可和官方下载提示，确认后由工具从 CipherTalk 官方 GitHub Release 下载安装程序。
3. 工具会核验固定 SHA-256；校验不一致时会停止并删除未通过校验的下载文件。
4. 校验成功后打开 CipherTalk 官方安装向导。
5. 按官方向导完成安装，不要强制结束安装程序。
6. 安装完成后启动一次 CipherTalk，等待主界面正常打开，使本机配置数据库被创建。
7. 退出 CipherTalk，再继续运行本包的 `setup.exe`。

支持版本与下载校验信息：

```text
产品：CipherTalk
版本：v2026.729.0
来源：https://github.com/ILoveBingLu/CipherTalk/releases/tag/v2026.729.0
安装器：CipherTalk-2026.729.0-Setup.exe
SHA-256：48354069b274591a2ca855fee8100addde3b0f75d05e8336ebb509f6a94bf88b
分发状态：本项目不内附或镜像该安装器
许可：CC BY-NC-SA 4.0，仅限学习和个人非商业用途
```

官方许可证原文保存在 `vendor\ciphertalk\LICENSE.txt`。下载后的安装程序只保存在当前电脑中，已被 Git 和 Release 构建规则排除。如果电脑已经安装同版或更新版 CipherTalk，可以跳过本节；不要为了使用本包而降级已正常工作的更新版本。

该 GitHub Release 安装程序没有 Windows Authenticode 数字签名，因此 Windows 可能显示 SmartScreen 或“未知发布者”提示。请先核对文件来源和上面的 SHA-256，再按照本人或所在组织的安全策略决定是否运行；不要关闭安全软件，也不要绕过组织管理员设置。无法接受未签名安装程序时，请自行从官方发布页获取后交由管理员审核安装。

## 五、放置便携文件夹

1. 如果收到 ZIP，先完整解压，不能直接在压缩包里运行。
2. 把文件夹放在长期固定的位置，例如：

   ```text
   D:\WechatRpaPortable
   ```

3. 不建议放在网盘同步目录、网络共享盘或以后会频繁移动的位置。
4. 配置完成后如果移动了文件夹，必须重新运行 `setup.exe` 保存一次。

## 六、首次配置 CipherTalk

确认 CipherTalk 已经安装并至少启动成功一次，然后退出 CipherTalk 和正在运行的影刀流程。配置工具会自动搜索常见安装目录和卸载注册表，通常会自动填写 CipherTalk 目录及启动程序；如果没有识别，再手动浏览选择。

双击便携文件夹根目录中的 `setup.exe`，按下面说明填写。

### 1. 路径配置

| 配置项 | 填写内容 |
|---|---|
| CipherTalk 目录 | CipherTalk 桌面版所在目录，或 CipherTalk 源码项目根目录 |
| CipherTalk 启动文件/命令 | 桌面版选择 `CipherTalk.exe`；源码版选择 `start-dev.bat`；根目录可自动识别时也可留空 |
| CipherTalk 配置数据库 | 通常为 `%APPDATA%\ciphertalk\ciphertalk-config.db` |
| 微信数据目录 | 当前微信账号的数据库目录，不是便携工具目录 |
| 微信账号 wxid | 当前账号的内部 wxid，例如 `wxid_...`，必须与数据库目录对应 |
| Excel 保存路径 | 建议保留默认位置，或选择本机固定目录中的 `.xlsx` 文件 |

CipherTalk 配置数据库必须已经存在。如果不存在，先正常启动一次 CipherTalk，再返回配置。

### 2. 密钥配置

| 配置项 | 要求 |
|---|---|
| 数据库密钥 | 必须是 64 位十六进制字符 |
| 图片 XOR 密钥 | 有则填写；没有可留空，但图片可能无法显示 |
| 图片 AES 密钥 | 有则填写；没有可留空，但图片可能无法显示 |

不要把密钥发到聊天群、截图或日志中。配置工具会把密钥保存在当前 Windows 用户可解密的 `config\secrets.dat` 中；为了让 CipherTalk 工作，也会按 CipherTalk 自身格式写入它的本机配置数据库。

### 3. 运行参数

- 轮询间隔：建议 `5` 秒，不要低于 `2` 秒。
- 每次消息上限：建议 `200`；消息量很大时可以提高，但不要超过配置工具允许的范围。

点击“保存并初始化”。工具会：

1. 生成本机 `config\app.json`。
2. 生成 DPAPI 加密的 `config\secrets.dat`。
3. 备份并更新 CipherTalk 配置数据库。
4. 创建空白 Excel 和采集状态文件。
5. 注册当前便携包位置，供影刀模块自动发现。

## 七、重启并检测环境

1. 完全退出 CipherTalk 后重新启动。
2. 双击 `doctor.exe`。
3. 重点确认以下项目通过：
   - `appConfig`
   - `encryptedSecrets`
   - `decryptKeyFormat`
   - `ciphertalkRoot`
   - `wechatDbPath`
   - `ciphertalkConfigDb`
   - `excelPath`
   - `excelWriter`
   - `shadowbot`
   - `ciphertalkMcp`

如果只有 `ciphertalkMcp` 失败，先确认 CipherTalk 正在运行、MCP 已启用，再核对微信数据库目录、wxid 和数据库密钥。

## 八、导入并修改影刀流程

先打开以下影刀分享页，获取“微信消息抓取3.0”并创建自己的流程副本：

```text
https://api.winrobot360.com/redirect/robot/share?inviteKey=dd2b99dec2af6ed9
```

分享流程不包含密钥、微信目录或消息数据。获取后仍必须按本节修改为接收者电脑上的实际路径和模块代码。

导入收到的影刀流程后，**不能直接运行**。流程中的程序路径通常保留制作方电脑的绝对地址，必须换成本机路径。

### 1. 修改采集程序路径

在无限循环开头找到第一个“运行或打开”指令，重新浏览选择：

```text
你的便携文件夹\collect_silent.exe
```

- 不填命令参数。
- 不填工作目录。
- 执行结果变量可以保留为 `process_info`。

### 2. 删除旧的“读取文件”指令

如果流程中存在“读取文件并保存到 `json_text`”，删除整条指令。

新版 `module1.py` 会根据本机配置自动读取：

```text
便携文件夹\state\new_messages.json
```

不再需要影刀单独读取 JSON。

### 3. 更新影刀自定义模块

1. 打开影刀的自定义模块/代码模块。
2. 打开便携文件夹中的 `shadowbot\module1.py`。
3. 复制其全部内容，覆盖影刀模块 `module1` 中的代码并保存。
4. “调用模块”选择：
   - 模块：`module1`
   - 函数：`main`
   - 参数 `args`：空字典 `{}`
   - 返回结果：`invoke_result`

`args` 必须是字典/对象/通用类型，不能是普通文本。如果影刀不能直接输入 `{}`，在循环之前创建变量：

```text
变量名：module_args
类型：字典/对象/通用
值：{}
```

调用模块时把 `module_args` 传给 `args`。

### 4. 设置返回变量

调用模块之后保留四个“设置变量”指令：

```text
parse_ok = invoke_result["parse_ok"]
excel_saved = invoke_result["excel_saved"]
message_count = invoke_result["message_count"]
parse_error = invoke_result["parse_error"]
```

变量类型：

| 变量 | 类型 |
|---|---|
| `invoke_result` | 字典/对象/通用 |
| `parse_ok` | 布尔 |
| `excel_saved` | 布尔 |
| `message_count` | 整数 |
| `parse_error` | 文本 |

### 5. 修改 ACK 程序路径

在成功分支中找到第二个“运行或打开”指令，重新浏览选择：

```text
你的便携文件夹\ack_silent.exe
```

- 不填命令参数。
- 不填工作目录。
- `ack_silent.exe` 只能放在 Excel 保存成功的分支中。

### 6. 正确的完整循环结构

```text
无限循环开始
    运行 collect_silent.exe
    等待 3 秒

    调用 module1.main
        args = {}
        返回 invoke_result

    parse_ok = invoke_result["parse_ok"]
    excel_saved = invoke_result["excel_saved"]
    message_count = invoke_result["message_count"]
    parse_error = invoke_result["parse_error"]

    IF parse_ok == True 并且 excel_saved == True
        打印日志：本轮新增消息数量 message_count
        运行 ack_silent.exe
        等待 1 秒
    ELSE
        打印日志：消息处理失败 parse_error
        等待 10 秒
        不执行 ACK
    End IF

    等待 5 秒
无限循环结束
```

`collect -> 调用模块 -> 判断保存 -> ACK -> 等待` 必须整体放在无限循环内部。启动微信或 CipherTalk的一次性动作可以放在循环外。

## 九、首次运行测试

1. 关闭目标 Excel 文件，避免文件锁。
2. 确认微信已经登录，CipherTalk 正在运行。
3. 启动影刀流程。
4. 第一轮用于建立当前会话基线，日志显示“本轮新增消息数量：0”是正常现象，不代表故障。
5. 基线完成后，用另一个账号向当前微信发送一条普通文本测试消息。
6. 等待一个或两个轮询周期。
7. 打开配置时选择的 Excel，确认出现新行。
8. 再测试一张图片，确认第 8 列出现预览或第 9 列出现本地媒体链接。

测试时不要连续多次手动启动影刀流程，避免同时运行多个循环实例。

## 十、日常使用

1. 登录微信。
2. 启动 CipherTalk；若配置允许，采集器也会尝试自动启动。
3. 启动影刀流程。
4. 需要查看 Excel 时可以暂停流程后打开；长时间打开 Excel 可能造成写入失败或只读锁。
5. 停止采集时，在影刀中停止流程，不要直接结束多个不明进程。

采集器采用 pending/ACK 机制：Excel 成功保存后才确认消息；保存失败时不会清空待写消息，解除文件占用后下一轮会重试。Excel 第一列的“消息键”还会进行二次去重。

## 十一、图片说明

图片预览需要同时满足：

1. CipherTalk 能提供媒体本地路径。
2. 图片 XOR/AES 密钥正确。
3. 对应媒体文件仍保存在本机，没有被微信清理。
4. 目标 Excel 没有被占用。

满足条件时，图片会尽量嵌入“图片预览”列，并在“媒体路径”列提供原文件链接。图片失败不会阻止文本消息写入。

## 十二、常见问题

### 1. 第一轮新增数量始终为 0

第一轮为基线是正常的。等待第一轮完成后再发送一条全新的测试消息。如果后续仍为 0，运行 `doctor.exe`，并确认 CipherTalk 会话接口可用。

### 2. `'str' object does not support item assignment`

调用 `module1.main` 时传入了文本。把 `args` 改为空字典 `{}`，或传入字典类型的 `module_args`。

### 3. `module 'xbot_robot.module1' has no attribute 'main'`

影刀模块中没有正确保存新版代码。重新完整复制 `shadowbot\module1.py`，确认存在：

```python
def main(args=None):
```

### 4. 提示找不到便携包

文件夹移动过，或当前 Windows 用户没有运行过配置。重新运行 `setup.exe` 并保存。

### 5. Excel 保存失败或文件只读

关闭 Excel、WPS 和资源管理器预览窗格中的目标工作簿，等待下一轮重试。失败分支不要执行 `ack_silent.exe`。

### 6. 图片没有预览

先看“媒体路径”列是否有内容。没有路径时核对 XOR/AES 密钥和 CipherTalk 的媒体路径设置；有路径但文件不存在，说明本地媒体可能已被清理。

### 7. CipherTalk MCP 检测失败

完全退出并重启 CipherTalk，确认其配置数据库路径正确。仍失败时核对微信数据库目录、wxid 和密钥。报告问题时只提供错误码和错误信息，不要发送任何密钥。

### 8. 移到另一台电脑后不能运行

这是正常的。新电脑必须：

1. 安装自己的依赖。
2. 运行自己的 `setup.exe`。
3. 输入自己的路径、wxid 和密钥。
4. 修改影刀中两个 EXE 的绝对路径。
5. 运行 `doctor.exe` 后再启动流程。

不能复制原电脑的 `secrets.dat` 代替配置，因为 DPAPI 与创建它的 Windows 用户绑定。

## 十三、重新配置或更换账号

1. 停止影刀流程。
2. 退出 CipherTalk。
3. 运行 `setup.exe`，填写新账号对应的数据库目录、wxid 和密钥。
4. 点击“保存并初始化”。这会重新建立基线，避免把旧账号状态混入新账号。
5. 重启 CipherTalk，运行 `doctor.exe`。
6. 测试一条新消息后再正式运行。

不同账号建议使用不同的便携文件夹和不同的 Excel 文件，隔离最清晰。

## 十四、分享和隐私清理

不要把已经运行过的个人工作目录直接压缩给别人。发送前必须排除：

```text
config\app.json
config\secrets.dat
state\collector_state.json
state\new_messages.json
logs\
data\wechat_messages.xlsx
任何微信数据库、媒体缓存和 CipherTalk 配置数据库
```

推荐发送项目生成的清理版 `wechat-rpa-portable-v1.2.zip`。该 ZIP 不包含 CipherTalk 安装程序；接收者必须从官方来源下载，并用自己的信息重新运行 `setup.exe`。

## 十五、验收清单

交付给新用户后，逐项确认：

- [ ] 便携文件夹已完整解压并放到固定目录。
- [ ] 微信已登录本人账号。
- [ ] 已通过 `1-安装密语.exe` 从官方来源下载并安装 CipherTalk，或确认电脑已有可用的同版/更新版。
- [ ] CipherTalk 至少成功启动一次。
- [ ] `setup.exe` 已用接收者自己的信息完成配置。
- [ ] `doctor.exe` 检测通过。
- [ ] 影刀第一个程序路径已改为本机 `collect_silent.exe`。
- [ ] 影刀已删除旧的“读取 JSON 文件”指令。
- [ ] 影刀 `module1` 已更新，`args` 为字典 `{}`。
- [ ] 影刀成功分支路径已改为本机 `ack_silent.exe`。
- [ ] ACK 只在 `parse_ok` 和 `excel_saved` 都为 `True` 时执行。
- [ ] 第一轮基线完成后，文本测试消息成功写入 Excel。
- [ ] 如需图片，图片测试已通过或媒体路径可点击。

完成以上项目后即可开始日常采集。
